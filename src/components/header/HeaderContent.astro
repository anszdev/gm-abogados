---
import Button from "@common/Button.astro";
---

<div class="gm_header-content">
  <h1 class="gm_pill">
    Abogado Despido Injustificado en CDMX | Asesoría Gratuita +150 Casos Ganados
  </h1>
  <h2 class="gm_header-title">
    Expertos en Despido Injustificado, No Pagas Si No Ganamos
  </h2>
  <p class="gm_header-text">
    Te despidieron sin liquidación, finiquito o prestaciones? Recupera hasta 20
    meses de salario. Primera consulta 100% gratuita y sin compromiso.
  </p>
  <div class="gm_header-button">
    <Button itsA="link" href="#contact"
      >Consulta Gratis Ahora - Sin Compromiso</Button
    >
  </div>
</div>

<style>
  .gm_header-content {
    animation: reveal 400ms ease-in-out;

    @media screen and (width >= 768px) {
      flex: 2;
      max-width: 50rem;
    }

    @media screen and (width >= 1200px) {
      max-width: 56rem;
    }
  }

  .gm_pill {
    background-color: rgba(31, 59, 90, 0.1);
    display: inline-block;
    font-size: 1rem;
    padding: 0.4rem 1rem;
    border-radius: 999px;
    color: var(--gm-primary);
    font-weight: 600;
    border: 2px solid rgba(255, 254, 254, 0.8);
  }

  .gm_header-title {
    font-family: var(--gm-family-title);
    color: var(--gm-title-text);
    font-size: 2rem;
    text-wrap: pretty;
    line-height: 1.2;
    margin-top: 1.5rem;
    font-weight: 700;

    @media screen and (width >= 768px) {
      font-size: 3rem;
    }

    @media screen and (width >= 1024px) {
      font-size: 4.5rem;
    }
  }

  .gm_header-text {
    font-size: 1.2rem;
    margin-top: 1.5rem;
    font-weight: 500;
    line-height: 1.25;

    @media screen and (width >= 768px) {
      max-width: 42rem;
      font-size: 1.4rem;
    }
  }

  .gm_header-button {
    padding-top: 2rem;
  }
</style>
