<h2 class="gm_section-title">
  <slot />
</h2>
<style>
  .gm_section-title {
    font-family: var(--gm-family-title);
    color: var(--gm-title-text);
    font-weight: 600;
    line-height: 1.1;
    text-wrap: pretty;
    font-size: 1.8rem;

    @media screen and (width >= 768px) {
      font-size: 2.8rem;
      max-width: 50rem;
    }

    @media screen and (width >= 1024px) {
      font-size: 4.3rem;
      max-width: 56rem;
    }
  }
</style>
